import { armLandingPage } from '../armPages'

export const dynamic = 'force-dynamic'

/** Arm A. Identical to the other arm except which video its watch page plays. */
export default function BookArmAPage() {
  return armLandingPage('A')
}
