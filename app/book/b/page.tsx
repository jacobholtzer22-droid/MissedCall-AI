import { armLandingPage } from '../armPages'

export const dynamic = 'force-dynamic'

/** Arm B. Identical to the other arm except which video its watch page plays. */
export default function BookArmBPage() {
  return armLandingPage('B')
}
