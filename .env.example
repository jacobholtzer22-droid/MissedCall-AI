# ===========================================
# MISSEDCALL AI - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git!

# Clerk Authentication
# Get these from: https://dashboard.clerk.com → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# Clerk URLs (don't change these)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Database (Neon PostgreSQL)
# Get these from: https://console.neon.tech → Connection Details
# Use pooled URL for app (DATABASE_URL) and direct URL for migrations (DIRECT_URL)
DATABASE_URL=postgresql://username:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DIRECT_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# Anthropic (Claude AI)
# Get this from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Telnyx
# Get these from: https://portal.telnyx.com
TELNYX_API_KEY=KEYxxxxx
TELNYX_PUBLIC_KEY=xxxxx
TELNYX_PHONE_NUMBER=+1xxxxxxxxxx
TELNYX_CONNECTION_ID=              # Call Control App ID (needed for call forwarding if not auto-detected from payload)

# Your app's base URL (update for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SMS Cooldown (optional) - days between automated messages to same contact (default: 7)
# Override per-business via Business.smsCooldownDays in database
SMS_COOLDOWN_DAYS=7

# Cron secret (optional) - for Vercel Cron to authenticate daily usage sync
# Set in Vercel env vars; must match Authorization: Bearer <CRON_SECRET>
CRON_SECRET=

# Admin user (optional) - Clerk user ID allowed to access /admin
ADMIN_USER_ID=

# Email notifications (Gmail SMTP) - for owner booking alerts
# For Gmail: use an App Password (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=   # Optional; defaults to SMTP_USER
